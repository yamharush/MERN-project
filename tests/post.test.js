

beforeAll(done => {
    done()
})

afterAll(done => {
    done()
})

describe("Posts Tests", () => {
    test("add new post", async () => {
        const tmp = 2
        expect(tmp).toEqual(2)
    })

    test("add new post", async () => {
        const tmp = 2
        expect(tmp).toEqual(2)
    })

    test("add new post", async () => {
        const tmp = 2
        expect(tmp).toEqual(3)
    })
})


